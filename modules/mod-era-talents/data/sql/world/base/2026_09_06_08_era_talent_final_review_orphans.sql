-- mod-era-talents: drop eight ORPHANED custom `spell_dbc` rows (final all-class review, R11).
--
-- Each id below belongs to a design the generator no longer emits, so `tools/era-regen.sh`
-- can never re-write (or re-delete) the row: it survives only on machines whose DB was
-- populated by an older generation. None of them is referenced by any YAML, generated SQL,
-- module source or client addon (`grep -rn <id> modules/mod-era-talents era-data
-- client-addons-src` = 0 hits outside "retired/free" prose), and no character holds one.
--
--   924288-924292  warrior Shield Specialization auto-passives, pre-Task-12 shape
--                  (replaced when the node moved to its current authoring)
--   928131/928132  warlock Fel Intellect pet ranks 4/5 (retired design — the pet chain
--                  ships 3 ranks)
--   932414         shaman "Elemental Mastery" cost half, retired 2026-08-23 when the old
--                  932413 crit + 932414 cost split collapsed into the single buff 932413
--                  (`era-data/vanilla/shaman.yaml:435` / `:1364` — "932414 remains free")
--
-- Idempotent: a plain id-scoped DELETE, a no-op once applied (and on any DB that never had
-- the rows). It deletes nothing a current generation writes.
DELETE FROM `spell_dbc` WHERE `Id` IN (924288,924289,924290,924291,924292,928131,928132,932414);
