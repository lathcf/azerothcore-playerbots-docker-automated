# Normalize line endings. Shell scripts MUST be LF so their shebang works when run
# inside WSL/Linux even if cloned on Windows (autocrlf). PowerShell/batch stay CRLF.
* text=auto eol=lf
*.sh   text eol=lf
*.ps1  text eol=crlf
*.bat  text eol=crlf
*.cmd  text eol=crlf
